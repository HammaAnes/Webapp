# ============================================================
# COFFICE — Configuration locale (ne jamais commiter ce fichier)
# ============================================================

# URL de l'API backend
# Option A (par défaut) : pointer sur la prod pour tester le frontend sans PHP local
VITE_API_URL=https://coffice.dz/api

# Option B : si tu as un serveur PHP local (XAMPP, Laragon, Valet…)
# VITE_API_URL=http://localhost/coffice-webapp/api
